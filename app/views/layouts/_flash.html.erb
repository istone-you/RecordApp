<% flash.each do |key, value| %>
  <div class="fixed-top mt-5 container">
    <div class="alert <%= key %> alert-dismissable fade show my-3">
      <%= value %>
      <button class="close" data-dismiss="alert">x</button>
    </div>
  </div>
<% end %>
