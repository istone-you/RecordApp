<h4 class="text-center folder-title"><span class="rec-fol mb-2"><% if @record.title %><%= @record.title %><% else %>No Title<% end%></span><br>を公開</h4>

<%= render 'form', public_record: @public_record %>

<%= link_to 'Back', public_records_path %>
