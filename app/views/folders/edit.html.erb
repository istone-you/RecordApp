
<div class= "index-header">
  <div class= "back-btn">
      <%= link_to image_tag("back.png"),folder_records_path(@folder), class: "d-inline-block  mb-3" %> 
  </div>
</div> 

<h3 class="text-center folder-title"><span class="rec-fol mr-2"><%= @folder.title %></span>を編集</h3>

<%= render 'form', folder: @folder %>

<div class="text-center d-inline-box pb-3">
<%= link_to '削除', @folder, method: :delete, data: { confirm: '削除しますか?' }, class: "r-btn" %>
</div>