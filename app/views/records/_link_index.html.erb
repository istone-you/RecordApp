    <div class="card-deck folreccard">
      <% @records.each do |record| %>
        <% if record.done == 1 %>
         <% if record.link? %>
          <div class="card-deck">
            <div class="card text-dark bg-light mb-3 folder mr-4 ml-4">
                  <div class ="">
                　  <a href="<%= record.link %>" class="">
                　      <img class="record-img" src="<%= record.image %>" width="198" height="auto">
                　  </a>
                　</div>  
            </div> 
          </div>
         <% end %>  
        <% end %> 
      <% end %>
    </div>